name: massagebook
description: "Messagebook"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get_it: ^7.6.7
  go_router: ^13.1.0
  http: ^1.2.0
  json_annotation: ^4.8.1
  freezed_annotation: ^2.4.1
  either_dart: ^1.0.0
  flutter_bloc: ^8.1.3
  html: ^0.15.4
  collection: ^1.18.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  json_serializable: ^6.7.1
  build_runner: ^2.4.8
  freezed: ^2.4.6


flutter:
  uses-material-design: true
  assets:
    - lib/fixtures.json
    - assets/

